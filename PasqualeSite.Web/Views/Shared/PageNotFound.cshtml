
@{
    ViewBag.Title = "The page you attempted to reach was not found.";
}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            @if (ViewBag.Error != null)
            {
                <p>@ViewBag.Error</p>
            }
            else
            {
                <p>Ah. The old "this page doesn't exist" situation. Either that, or you are just typing random stuff in the URL. Either way, find your way back <a src="@Url.Action("Index", "Home")">here</a>.</p>
            }
        </div>
    </div>
</div>
